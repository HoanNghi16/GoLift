import { colorType } from "@/types/color";

export const LIGHT_COLORS: colorType = {
  background: "#f1f1f1",   // đen chủ đạo
  surface: "#e9e9e9",      // đen nhạt hơn 1 chút, dùng cho card/section
  primary: "#c2410c",      // cam tối (đậm)
  primaryText: "#ea580c",  // cam tối nhưng sáng hơn 1 chút, dùng cho chữ nhấn
  textPrimary: "#080808",
  textSecondary: "#2e2e2e",
};
export const DARK_COLORS : colorType = {
  background: "#0d0d0d",   // đen chủ đạo
  surface: "#1a1a1a",      // đen nhạt hơn 1 chút, dùng cho card/section
  primary: "#c2410c",      // cam tối (đậm)
  primaryText: "#ea580c",  // cam tối nhưng sáng hơn 1 chút, dùng cho chữ nhấn
  textPrimary: "#ffffff",
  textSecondary: "#9a9a9a",
};